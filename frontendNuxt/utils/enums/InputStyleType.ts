export enum SelectionType {
    Grid = 'selectionGrid',
    FreeStanding = 'SelectionFree',
}

enum InputStyleType {
    Dark = 'inputDark',
    Light = 'inputLight',
}

export default InputStyleType;
